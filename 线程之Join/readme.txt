最近面试遇到一个常见的面试题：
	存在两个线程t1 和 t2，请问t1.start()和t2.start()在执行的时候，是否有顺序；若没有顺序，那么怎么才能变得有顺序？

答案肯定是无序的，换个角度想，如果有序的话第二问毫无意义。
言归正传，线程运行是无序的，
通常情况下 main是主线程，thread是子线程，在占用cpu执行的顺序上，是谁先占用到空间谁先执行，多线程在不控制的情况下是无序的
而控制就是通过join()方法来控制先后，这次给的demo就是join()的用法。

大家可以将有join()和无join()的进行对比，答案就一目了然了。
有问题，欢迎留言。